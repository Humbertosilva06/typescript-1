// para criar o script fizemos no package json o start e comolcamos tsc index.ts && node index.js, assim ao roda-lo ele transpila o arquivo ts para js (faz uma copia para js do mesmo código e depois executa no terminal)

console.log("helloworls");
console.log ("testando")
